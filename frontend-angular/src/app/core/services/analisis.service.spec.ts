import { TestBed } from '@angular/core/testing';import { HttpClientTestingModule,HttpTestingController } from '@angular/common/http/testing';import { AnalisisService } from './analisis.service';
describe('AnalisisService',()=>{let service:AnalisisService,http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({imports:[HttpClientTestingModule]});service=TestBed.inject(AnalisisService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('envia el Excel y contexto al backend sin calcular',()=>{const archivo=new File(['x'],'prueba.xlsx');service.cargar(archivo,{datos_cliente:{cliente_cuit:'1'}}).subscribe();const req=http.expectOne('http://localhost:8001/api/analisis/excel');expect(req.request.method).toBe('POST');expect(req.request.body instanceof FormData).toBeTrue();req.flush({estado:'analisis_completado'});});});
