import { Pipe,PipeTransform } from '@angular/core';export function formatearFechaHora(fecha:unknown):string{const d=new Date(String(fecha));if(Number.isNaN(d.getTime()))return'No disponible';return new Intl.DateTimeFormat('es-AR',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit',hour12:false}).format(d).replace(',','');}@Pipe({name:'fechaAr'})export class FechaArPipe implements PipeTransform{transform(v:unknown){return formatearFechaHora(v);}}
