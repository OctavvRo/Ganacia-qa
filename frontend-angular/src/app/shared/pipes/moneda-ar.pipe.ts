import { Pipe,PipeTransform } from '@angular/core';@Pipe({name:'monedaAr'})export class MonedaArPipe implements PipeTransform{transform(valor:unknown):string{if(valor===null||valor===undefined||valor==='')return'No disponible';const n=Number(valor);return Number.isFinite(n)?new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS',minimumFractionDigits:2}).format(n):'No disponible';}}
